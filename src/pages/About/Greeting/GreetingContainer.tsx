import React from "react";
import GreetingPresentation from "./GreetingPresentation";

const GreetingContainer: React.FC = () => {
  return <GreetingPresentation />;
};

export default GreetingContainer;
